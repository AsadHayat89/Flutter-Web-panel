import 'package:flutter/material.dart';

class ReferalInfoModel {
  final String? message, title;
  final String? time;
  final Color? color;

  ReferalInfoModel({
    this.message,
    this.title,
    this.time,
    this.color,
  });
}

